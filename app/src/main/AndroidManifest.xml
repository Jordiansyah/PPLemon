<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gs.anastasia.mandala">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainMenu"
            android:label="@string/app_name" />
        <activity android:name=".SelectionMenu" />
        <activity android:name=".AboutMandala" />
        <activity
            android:name=".SettingMenu"
            android:label="@string/title_activity_setting_menu" />
        <activity android:name=".MandalaMenu" />
        <activity android:name=".MandalaFragment" />
        <activity
            android:name=".splashScreen.SplashScreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".lesson.Basic1" />
        <activity
            android:name=".lesson.insideLesson.insLesson1"
            android:label="@string/title_activity_ins_lesson1"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>